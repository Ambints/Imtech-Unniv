import { Repository, DataSource } from 'typeorm';
import { User } from './user.entity';
import { SuperAdmin } from './super-admin.entity';
import { Tenant } from '../tenants/tenant.entity';
import { EmailService } from '../email/email.service';
import { CacheService } from '../cache/cache.service';
export declare class UsersService {
    private repo;
    private superAdminRepo;
    private tenantRepo;
    private dataSource;
    private emailService;
    private cacheService;
    constructor(repo: Repository<User>, superAdminRepo: Repository<SuperAdmin>, tenantRepo: Repository<Tenant>, dataSource: DataSource, emailService: EmailService, cacheService: CacheService);
    private generateSecurePassword;
    create(dto: any): Promise<any>;
    findSuperAdminById(id: string): Promise<SuperAdmin | null>;
    updateSuperAdminPassword(id: string, hashedPassword: string): Promise<void>;
    findById(id: string): Promise<User | null>;
    updateUserPassword(id: string, hashedPassword: string): Promise<void>;
    findAll(tid?: string, role?: string, university?: string, page?: number, limit?: number): Promise<any[]>;
    findOne(id: string): Promise<any>;
    findByEmail(email: string): Promise<any>;
    getTenantInfo(tenantId: string): Promise<Tenant | null>;
    findSuperAdminByEmail(email: string): Promise<SuperAdmin | null>;
    updateSuperAdminLastLogin(id: string): Promise<void>;
    update(id: string, dto: any): Promise<any>;
    remove(id: string): Promise<void>;
    updateRefreshToken(id: string, token: string | null): Promise<void>;
}
